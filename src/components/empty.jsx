const EmptyView = () => {
  return (
    <section className="flex items-center w-full h-full place-content-center rounded-r-2xl bg-primary-100">
      <p>Please select a note from the left.</p>
    </section>
  );
};

export default EmptyView;
